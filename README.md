# Sociophilia

To run the app:
Need to set appropriate ip addr for exposing from server to outside with set_ip_addr_env_var.sh
Need JDK 17
Need Android Studio
Need to set ANDROID_HOME and JAVA_HOME env vars